<h1>This is all the posts</h1>

<table>
	<tr>
		<td>URL</td>
		<td>Title</td>
		<td>Comments</td>
	</tr>

<% @posts.each do |post| %>
	<tr>
		<td><%= post.url %></td>
		<td><%= link_to post.title, post_path(post) %></td>
		<td><%= post.comments.count %></td>
	</tr>
<% end %>
</table>